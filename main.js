const app = Vue.createApp({
    data(){
        return {
            product: 'Socks',
            description: 'Feet warmers, comes in pairs. To be worn inside shoes.'
        }
    }
});

// const product = 'Socks'
